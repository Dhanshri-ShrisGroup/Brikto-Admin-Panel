import 'package:flutter/material.dart';

class AppColors {
  // 🟤 Background (Concrete / Blueprint paper)
  static const Color background = Color(0xFFF4F5F7);

  // 🟧 Primary (Construction Orange – helmets, machinery)
  static const Color primary = Color.fromARGB(255, 0, 161, 132);

  // 🟫 Secondary (Steel / Concrete dark)
  static const Color secondary = Color(0xFF2C2C2C);

  // ⬜ Card background
  static const Color cardBackground = Colors.white;

  // 🖤 Main text
  static const Color textPrimary = Color(0xFF2C2C2C);

  // 🩶 Secondary text
  static const Color textSecondary = Color(0xFF7A7A7A);

  // 🟩 Status colors (optional but useful)
  static const Color success = Color(0xFF2ECC71);
  static const Color danger = Color(0xFFE74C3C);
  static const Color warning = Color(0xFFF39C12);
}
